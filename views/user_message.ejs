<% include header %>

<section class="content">
    <header class="content-header">
        <h1 class="light no-margin"><%= user.name.first %> <%= user.name.last %></h1>
		<h2 class="light no-margin">Message</h2>
    </header>
    <section class="bordered">
		<form method="post" action="/<%= user.username %>/user_message/" class="form-skin">
			<fieldset>
			  <label>Post a message:</label>
			  <input id="cID" type="text" name="cID" value="<%= cID %>" />
			  <input id="oID" type="text" name="oID" value="<%= oID %>" />
			  <textarea id="message" name="message" rows="4" cols="50"></textarea>
			</fieldset>						
			<p>
			  <input id="post" type="button" value="Post Message" class="btn btn-flat btn-blue no-radius"/>
			</p>
		</form>
		<ul>
		<% for(var i=0; i<messageArray.length; i++) {%>
			<li>
			<%= messageArray[i].username %> <%= messageArray[i].message %>
			</li>
		<% } %>
		</ul>		
    </section>
</section>

<% include footer %>

  <script type="text/javascript">
	var markup;
      $('#post').click(function(){ 
      	console.log("post clicked"); 
		var messageString = $("#message").val(),
			cIDString = $("#cID").val(),
			oIDString = $("#oID").val();
			
		console.log("oIDString "+oIDString);
		if((messageString.length > 0) && (cIDString.length > 0)) {
		     $.ajax({ 
		           url: '/<%= user.username %>/user_message/',
		           type: 'POST',
		           cache: false, 
		           data: { message: messageString, cID: cIDString }, 
		           success: function(data){
		           	  console.log(typeof(data));
		           	  markup = data;
		              //alert('Success!');
		              $("section ul").append(data);
		              
		           }
		           , error: function(jqXHR, textStatus, err){
		               alert('text status '+textStatus+', err '+err)
		           }
		        });
		}
		if((messageString.length > 0) && (oIDString.length > 0)) {
		     $.ajax({ 
		           url: '/<%= user.username %>/user_message/',
		           type: 'POST',
		           cache: false, 
		           data: { message: messageString, cID: oIDString }, 
		           success: function(data){
		           	  console.log(typeof(data));
		           	  markup = data;
		              //alert('Success!');
		              $("section ul").append(data);
		              
		           }
		           , error: function(jqXHR, textStatus, err){
		               alert('text status '+textStatus+', err '+err)
		           }
		        });
		}		
     });            

  </script>