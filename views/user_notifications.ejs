<% include header %>

<section class="content">
    <header class="content-header">
        <h1 class="light no-margin"><%= user.name.first %> <%= user.name.last %></h1>
		<h2 class="light no-margin">Notifications</h2>
    </header>
    <section class="bordered">
		<ul>
		<% for(var i=0; i<user.check_in.check_in_message.message_thread.length; i++) {%>
			<li>
			<form id="form<%= i %>" method="post" action="/<%= user.username %>/user_message/" class="form-skin">
			
			<%= user.check_in.check_in_message.message_thread[i].username %>: <%= user.check_in.check_in_message.message_thread[i].message %>
			<fieldset>
			  <input id="oID" type="text" name="oID" value="<%= user.check_in.check_in_message.message_thread[i].oID %>" />
			</fieldset>
			<p>
			  <input type="submit" value="Message" class="btn btn-flat btn-blue no-radius"/>
			</p>	
			</form>
			</li>
		<% } %>
		</ul>
    </section>
</section>

<% include footer %>